# Example environment variables for backend_django_api
# For local development, defaults are sufficient (SQLite). No variables required.
# SECRET_KEY, DEBUG, DATABASE_URL, etc. can be added for production setups.
